# Core dependencies for fine-tuning, quantization, and evaluation
torch>=2.2.0
transformers>=4.48.0
peft>=0.13.0
trl>=0.12.0
datasets>=3.0.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
huggingface_hub>=0.26.0

# Inference & Benchmarking
requests>=2.31.0
psutil>=5.9.0

# Optional local GGUF inference
# pip install llama-cpp-python
