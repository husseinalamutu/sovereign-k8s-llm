apiVersion: v1
kind: Service
metadata:
  name: sovereign-llm-service
  namespace: sovereign-ai
  labels:
    app: sovereign-llm
spec:
  type: ClusterIP
  selector:
    app: sovereign-llm
  ports:
  - port: 8080
    targetPort: 8080
    name: http
    protocol: TCP
